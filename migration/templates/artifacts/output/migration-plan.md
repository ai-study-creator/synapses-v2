# Migration Plan

Status: `<DRAFT / APPROVED>`

This plan is based on `system-map.md`, `requirements.md`, and `stack/tech-stack-source.md`.

## Dependency Order

1. `<slice-name>` - `<why first>`
2. `<slice-name>` - `<dependency reason>`

## Implementation Slices

| Order | Slice | Includes | Depends on | Primary tests |
|---:|---|---|---|---|
| `1` | `<slice-name>` | `<requirements or area>` | `<dependency or none>` | `<test strategy>` |

## Current Status

| Slice | Status |
|---|---|
| `<slice-name>` | `<Planned / In progress / Implemented / Approved / Deferred>` |

## Test Strategy

| Area | Test type |
|---|---|
| `<area>` | `<Acceptance / Contract / Integration / Unit / Golden master / Manual validation>` |

## Resolved Planning Decisions

- `<decision>`

## Recommended Next Slice

`<slice-name>` because `<reason>`.
